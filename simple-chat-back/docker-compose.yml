version: "3.3"

services:
  server:
    container_name: simple_chat_back
    build:
      context: ../
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3000:3000
    env_file:
      - .env
    volumes:
      - ./:/usr/src/
    command: ["npm", "run", "dev"]
